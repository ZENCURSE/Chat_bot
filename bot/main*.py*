from pyrogram import Client
from config import API_ID, API_HASH, BOT_TOKEN
app = Client("coderips-superbot", api_id=API_ID, api_hash=API_HASH, bot_token=BOT_TOKEN, plugins=dict(root="bot/plugins"))
if __name__ == "__main__":
    print("Coderips SuperBot Started 🚀")
    app.run()
